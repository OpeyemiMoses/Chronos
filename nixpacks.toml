providers = ["python"]

[phases.setup]
nixPkgs = ["python3", "pip"]

[phases.install]
cmds = ["python3 -m pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "python3 server.py"
